# Catatan singkat

- Nokos sekarang punya `can_cancel_now(order)` dan `cancel_order`.
- Jendela cancel: 180 detik sejak nomor dikirim (bisa diatur `NOKOS_CANCEL_WINDOW`).
- Setelah OTP keluar, cancel tidak bisa.
- Demo negara placeholder: lihat `list_negara()`.
